# apiVersion: v1
# kind: Secret
# metadata:
#   name: "{{ .Release.Name }}-secret"
# type: Opaque
# data:
#   # If you have secrets, encode them as base64 and insert here
#   # For example:
#   # SOME_SECRET: <base64-encoded-value>
